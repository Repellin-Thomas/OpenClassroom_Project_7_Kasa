function Rating(props) {

    const ratingRange = 5
    const fullStars = parseInt(props.rating)
    const emptyStars = ratingRange - fullStars
    //boucle for qui retourne 


    return (<div className="rating-container"></div>)
}